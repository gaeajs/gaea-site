{
  "name": "gaea-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pri",
    "build": "pri build",
    "preview": "pri preview",
    "deploy": "npm run build && && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "gh-pages": "^1.1.0",
    "typescript": "^2.7.2"
  },
  "dependencies": {
    "pri": "0.0.41"
  }
}